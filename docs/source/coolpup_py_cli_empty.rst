coolpup.py CLI
==============

Run ``coolpup.py`` to generate pileups and ``plotpup.py`` to generate a figure with one or multiple pileups.

coolpup.py command
------------------

In progress...

plotpup.py command
------------------

In progress...
